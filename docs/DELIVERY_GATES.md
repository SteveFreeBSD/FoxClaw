# Delivery Gates

This document points to the current gate contracts and pre-merge checks:

- `docs/QUALITY_GATES.md`
- `docs/PREMERGE_READINESS.md`

Use those files as the source for required local and CI gate commands.
