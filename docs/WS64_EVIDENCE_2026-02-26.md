# WS-64 Evidence (2026-02-26)

## Objective
Provide auditable proof for WS-64 completion gates:
- full quality/security/test gates green,
- windows-share mini soak passes without operational failures,
- no open critical/high contract findings in current review packet.

## Evidence Runs

1. Full certification gate
- Command: `./scripts/certify.sh`
- Result: pass
- Key checks included:
  - `ruff check .`
  - `mypy foxclaw`
  - `pytest -q -m "not integration"`
  - `pytest -q -m integration`
  - `bandit`, `vulture`, `./scripts/check_secrets.sh`

2. Full test suite
- Command: `.venv/bin/pytest -q`
- Result: `199 passed`

3. Windows-share mini soak (fixture-based)
- Command:
  - `.venv/bin/python -m foxclaw acquire windows-share-batch --source-root /tmp/foxclaw-ws64-source --staging-root /tmp/foxclaw-ws64-stage --out-root /tmp/foxclaw-ws64-out --max 3 --workers 1 --treat-high-findings-as-success`
- Result:
  - `EXIT_CODE=0`
  - summary path: `/tmp/foxclaw-ws64-out/windows-share-batch-summary.json`
  - summary values:
    - `attempted=3`
    - `clean_count=3`
    - `findings_count=0`
    - `operational_failure_count=0`

## Supporting Contract Anchors
- WS-64 definition and completion criteria (`docs/WORKSLICES.md:836-844`).
- Premerge gate contract (`docs/PREMERGE_READINESS.md:5-60`).
- Gate command source (`scripts/certify.sh:64-151`).
