# WS-72 Evidence (2026-02-27)

WS-72 goal: merge the validated Python baseline to mainline, prove the merge target is green, and hand Rust work off to a dedicated branch.

## Merge Target

- merge candidate branch: `ws72-mainline-merge`
- source branch: `docs/windows-profile-gen`
- post-merge Rust branch: `rust/ws31-bootstrap`

## Merge-Target Gate Results

1. Base certification

```bash
./scripts/certify.sh
```

Result: pass

2. Live-profile certification

```bash
./scripts/certify.sh --with-live-profile --profile tests/fixtures/firefox_profile
```

Result: pass

- live diff drift_detected: `False`
- live diff added/removed/changed: `0 0 0`

3. Dependency audit

```bash
make dep-audit
```

Result: pass

- `packages_with_vulns=0`
- `vulnerabilities_total=0`

4. Packaging dry-run and install smoke

```bash
.venv/bin/python -m build
.venv/bin/python -m twine check dist/*
python3 -m venv "<tmp>/venv"
"<tmp>/venv/bin/pip" install dist/*.whl
"<tmp>/venv/bin/foxclaw" --help
```

Result: pass

5. SBOM generation and verification

```bash
make sbom
make sbom-verify
```

Result: pass

- `spec=1.6`
- `components=51`
- `foxclaw_version=0.1.0`

6. Full regression

```bash
.venv/bin/pytest -q
```

Result:

- `265 passed, 7 skipped`

## Outcome

- The Python baseline is validated for mainline.
- `main` remains the clean source of truth.
- Rust execution resumes next from the dedicated branch `rust/ws31-bootstrap`, not from `main`.
