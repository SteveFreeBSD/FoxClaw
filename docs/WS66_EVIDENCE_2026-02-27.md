# WS-66 Evidence (2026-02-27)

WS-66 goal: prove the Python implementation is the clean source of truth before any Rust branch work begins.

## Gate Results

1. Base certification

```bash
./scripts/certify.sh
```

Result: pass

2. Live-profile certification

```bash
./scripts/certify.sh --with-live-profile --profile tests/fixtures/firefox_profile
```

Result: pass

- live diff drift_detected: `False`
- live diff added/removed/changed: `0 0 0`

3. Dependency audit

```bash
make dep-audit
```

Result: pass

- `packages_with_vulns=0`
- `vulnerabilities_total=0`

4. Packaging dry-run and install smoke

```bash
.venv/bin/python -m build
.venv/bin/python -m twine check dist/*
python3 -m venv "<tmp>/venv"
"<tmp>/venv/bin/pip" install dist/*.whl
"<tmp>/venv/bin/foxclaw" --help
```

Result: pass

5. SBOM generation and verification

```bash
make sbom
make sbom-verify
```

Result: pass

- `spec=1.6`
- `components=51`
- `foxclaw_version=0.1.0`

6. Short soak confidence pass

```bash
scripts/soak_runner.sh \
  --duration-hours 1 \
  --max-cycles 1 \
  --integration-runs 1 \
  --snapshot-runs 1 \
  --synth-count 2 \
  --synth-mode bootstrap \
  --synth-seed 424242 \
  --synth-mutation-budget 0 \
  --synth-fidelity-min-score 70 \
  --require-launch-gate \
  --launch-gate-min-score 50 \
  --fuzz-count 2 \
  --fuzz-mode chaos \
  --fuzz-seed 525252 \
  --fuzz-mutation-budget 1 \
  --fuzz-fidelity-min-score 50 \
  --adversary-runs 1 \
  --adversary-count 1 \
  --matrix-runs 1 \
  --label ws66-premerge
```

Result: pass

- run dir: `/var/tmp/foxclaw-soak/20260227T135950Z-ws66-premerge`
- `cycles_completed=1`
- `steps_total=16`
- `steps_failed=0`
- `overall_status=PASS`

7. Stopped comprehensive soak review (March 1, 2026)

Command launch:

```bash
systemd-run --user \
  --unit foxclaw-soak-comprehensive \
  --same-dir \
  --collect \
  scripts/soak_runner.sh \
    --duration-hours 10 \
    --stage-timeout-seconds 1800 \
    --siem-wazuh-runs 1 \
    --label comprehensive-precommit \
    --output-root /var/tmp/foxclaw-soak
```

Observed run:

- run dir: `/var/tmp/foxclaw-soak/20260301T090911Z-comprehensive-precommit`
- stopped manually for evidence review at `2026-03-01T13:46:42Z`
- `cycles_completed=37`
- `steps_total=899`
- `steps_passed=898`
- `steps_failed=1`
- `stop_reason=signal`
- `overall_status=FAIL`

Observed `soak-summary.json` showed all completed production lanes clean before stop:

- `integration`: `190 pass / 0 fail`
- `snapshot`: `189 pass / 1 fail`
- `trust_scan`: `37 pass / 0 fail`
- `synth`: `37 pass / 0 fail`
- `fuzz`: `37 pass / 0 fail`
- `siem_wazuh`: `37 pass / 0 fail`
- matrix ESR/beta/nightly build/version/scan: `37 pass / 0 fail` each

Findings from the stopped run:

- The sole recorded failure was `cycle=38 stage=snapshot iter=3 ec=143`.
- `run.log` shows that failure occurred immediately after:
  - `Stop signal received; ending soak after current step.`
- `cycle-38-snapshot-3.log` is empty, which is consistent with an interrupted stage rather than an organic product regression.
- Conclusion: the March 1 comprehensive soak did not surface a core scan/runtime failure; it surfaced a soak control-plane gap where an operator-requested stop is currently recorded as a synthetic stage failure.

8. Full comprehensive soak rerun after WS-82 (March 1-2, 2026)

Command launch:

```bash
systemd-run --user \
  --unit foxclaw-soak-comprehensive \
  --same-dir \
  --collect \
  scripts/soak_runner.sh \
    --duration-hours 10 \
    --stage-timeout-seconds 1800 \
    --siem-wazuh-runs 1 \
    --label comprehensive-precommit-ws82 \
    --output-root /var/tmp/foxclaw-soak
```

Observed run:

- run dir: `/var/tmp/foxclaw-soak/20260301T140921Z-comprehensive-precommit-ws82`
- `started_at=2026-03-01T14:09:21Z`
- `ended_at=2026-03-02T00:12:50Z`
- `duration_seconds=36209`
- `cycles_completed=80`
- `steps_total=1920`
- `steps_passed=1920`
- `steps_failed=0`
- `steps_interrupted=0`
- `stop_reason=deadline`
- `overall_status=PASS`

Observed `soak-summary.json` confirmed every completed production lane stayed clean:

- `integration`: `400 pass / 0 fail / 0 interrupted`
- `snapshot`: `400 pass / 0 fail / 0 interrupted`
- `snapshot_hash_check`: `80 pass / 0 fail / 0 interrupted`
- `trust_scan`: `80 pass / 0 fail / 0 interrupted`
- `synth`: `80 pass / 0 fail / 0 interrupted`
- `fuzz`: `80 pass / 0 fail / 0 interrupted`
- `siem_wazuh`: `80 pass / 0 fail / 0 interrupted`
- matrix ESR/beta/nightly build/version/scan: `80 pass / 0 fail / 0 interrupted` each

Observed mounted-share presoak before the long run:

- `scripts/windows_share_preflight.sh /mnt/firefox-profiles` reported `fstype=autofs cifs` and `profiles_count=53`
- direct staged scan of `foxclaw-gen-001.default` completed and wrote all expected artifacts under `/var/tmp/foxclaw-presoak-share-ws82`
- bounded `windows-share-batch` sanity pass completed cleanly:
  - summary: `/var/tmp/foxclaw-share-batch-precommit-ws82/windows-share-batch-summary.json`
  - `attempted=5`
  - `clean_count=5`
  - `findings_count=0`
  - `operational_failure_count=0`
  - generated-profile runtimes over the mounted CIFS path ranged from `127.977` to `176.159` seconds, with no operational failures

Findings from the successful rerun:

- WS-82 resolved the false-failure problem from the earlier stopped soak; the next long run completed without any `FAIL` or `INTERRUPTED` rows.
- No new Python product regressions were observed across integration, determinism, trust, synth, fuzz, Wazuh, or Firefox matrix lanes.
- The mounted Windows-share corpus remains operationally clean, but the presoak data shows staging latency on the current CIFS path is materially variable and much slower than local fixtures.
- Operational lesson: keep the mounted-share presoak gate in front of long soaks, because it catches share-path issues early and establishes realistic staging/runtime expectations before spending 10 hours on the full harness.

9. Full pre-merge gate rerun on the current tree (March 2, 2026)

Commands:

```bash
./scripts/certify.sh
./scripts/certify.sh --with-live-profile --profile tests/fixtures/firefox_profile
make dep-audit
.venv/bin/python -m build
.venv/bin/python -m twine check dist/*
python3 -m venv "<tmp>/venv"
"<tmp>/venv/bin/pip" install dist/*.whl
"<tmp>/venv/bin/foxclaw" --help
make sbom
make sbom-verify
```

Result: pass

- `./scripts/certify.sh`: pass
- `./scripts/certify.sh --with-live-profile --profile tests/fixtures/firefox_profile`: pass
  - live diff drift_detected: `False`
  - live diff added/removed/changed: `0 0 0`
- `make dep-audit`: pass
  - `packages_with_vulns=0`
  - `vulnerabilities_total=0`
- packaging dry-run + wheel install smoke: pass
- `make sbom`: pass
- `make sbom-verify`: pass
- rerun conclusion: the current post-WS-82/WS-83 tree remains gate-clean on substance; the remaining hold is still commit hygiene, not runtime or release readiness.

## Findings Review

- No residual product failures were observed in the short soak.
- The previously observed overnight matrix failures were infrastructure/bootstrap drift in the container lane, not Python scan logic regressions.
- After container bootstrap hardening, ESR, beta, and nightly matrix scan lanes all passed in the short soak.
- The March 1 stopped comprehensive soak extended that confidence: 37 full cycles completed with no organic failures across integration, snapshot determinism, trust, synth, fuzz, Wazuh, or Firefox matrix lanes before the manual stop.
- WS-82 closed the only new blocker from the stopped run by making operator-requested stops report `INTERRUPTED` instead of `FAIL`.
- The March 1-2 comprehensive rerun extended confidence further: 80 full cycles completed cleanly with `1920/1920` passing steps and zero failures across all production lanes.
- No new product blockers were identified by the latest comprehensive soak.
- The remaining improvement opportunity is procedural rather than corrective: mounted-share staging over CIFS is materially slower and more variable than local fixtures, so the presoak gate should remain mandatory ahead of long soaks.
- The earlier operator-sequencing gap is now closed by WS-83, which added `scripts/windows_share_comprehensive_soak.py` as the first-class mounted-share comprehensive soak entrypoint.
- A full March 2 pre-merge gate rerun kept the current tree clean across certify, dependency audit, packaging, and SBOM verification.

## Merge Recommendation

- Python is gate-clean and remains the canonical source of truth.
- Do not merge the current worktree as a single unit yet.
- Reasons:
  - the repository still contains mixed in-flight changes from multiple slices and review scopes; those changes should be isolated into coherent commits or PRs before merge.
  - the runtime and soak workflow blockers identified during WS-66 are now closed, so the remaining hold is repository hygiene rather than missing Python functionality.
